class Solution {
public:
    //from my observation there are three possibilities
    //1->just one child has two parents then return the current edge
    //2->one child can have two parents but return which edge contains cycle
    //3->one parent can have three child just return which edge
    //contributes have two parents
    vector<int> findRedundantDirectedConnection(vector<vector<int>>& edges){
        
    }
};